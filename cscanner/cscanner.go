package cscanner

import (
	"log"
)

// ConnectionScanner returns a map of tcp connections from the input tokens
// that have a unique(srcIP:srcPort, dstIP:dstPort).
func ConnectionScanner() {
	log.Printf("logic to count / filter tokens after parsing")
}
